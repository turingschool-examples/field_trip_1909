<h1><%= @airline.name %></h1>

<h2>Passengers</h2>
<% @airline.flights.each do |fly| %>
  <% fly.passengers.each do |pas| %>
  <p><%= pas.name %></p>
  <% end %>
<% end %>

<% @airline.flights.each do |flight| %>
  <section id = "flight-<%=flight.id%>">
    <h2>Flight Number: <%= flight.number %></h2>
    <p>Date: <%= flight.date %></p>
    <p>Time: <%= flight.time %></p>
    <p>Departure City: <%= flight.departure_city %></p>
    <p>Arrival City: <%= flight.arrival_city %></p>
  </section>
<% end %>
